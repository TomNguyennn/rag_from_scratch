# Install with:
#   python -m pip install -r requirements.txt
#
# Notebook note:
#   The notebook uses `%pip install -r requirements.txt` so packages land in the
#   active kernel environment.
#
# Selenium note:
#   `SeleniumURLLoader` also needs a local browser and matching driver installed
#   outside pip.

# Notebook runtime
ipykernel>=6.29,<8.0
notebook>=7.0,<8.0
ipywidgets>=8.1,<9.0
python-dotenv>=1.0,<2.0

# Core RAG / LangChain stack
langchain-core>=1.2,<2.0
langchain-classic>=1.0,<2.0
langchain-community>=0.4,<0.5
langchain-text-splitters>=1.1,<2.0
langchain-google-genai>=4.2,<5.0
langchain-huggingface>=1.2,<2.0
langsmith>=0.7,<1.0
google-genai>=1.67,<2.0

# Retrieval, embeddings, and vector store
chromadb>=1.5,<2.0
sentence-transformers>=5.3,<6.0
rank-bm25>=0.2,<1.0
tiktoken>=0.12,<1.0

# Data and parsing
beautifulsoup4>=4.12,<5.0
numpy>=1.26,<3.0
pydantic>=2.10,<3.0
selenium>=4.41,<5.0
spacy>=3.8,<4.0
en-core-web-sm>=3.8,<3.9
